# Example output: /prime

Prompt:

```text
/prime
```

Result:

- Current task: add a stronger sample that demonstrates project and task creation.
- Relevant app: `sample/project-tasks-app/`.
- Test runner: `cd sample/project-tasks-app && npm test`.
- Existing workflow surface: `/prime-module`, `/grill`, `/plan`, `/execute`, `/eval`, `/review`, `/memory-update`, `/maintain-memory`.
- Recommended next step: `/prime-module api`, then `/grill build project and task creation`.

Why it matters: the agent starts with repo state, test commands, and the likely module before asking requirements.
