# Example output: /prime

Prompt:

```text
/prime
```

Result:

- Current branch has local sample workflow changes; do not commit until README and smoke tests are updated.
- Recent work added copy-mode install, hook smoke tests, and project memory templates.
- Stale flags: none for the demo app.
- Available workflow docs: `.cursor/docs/patterns/workflow.md`, `.cursor/docs/modules/api.md`.
- Recommended next step: `/prime-module api`, because the duplicate email bug is likely in request handling.

Why it matters: `/prime` gives a new agent enough session context to avoid wandering.
