# Open questions — project-tasks

- Should real apps use workspace-wide roles, project-specific roles, or both?
- Should audit logs be synchronous, queued, or written transactionally with the mutation?
